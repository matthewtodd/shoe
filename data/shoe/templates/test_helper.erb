require 'test/unit'
require '<%= name %>'

# NOTE: If you can help it, stick to using the Ruby standard library in your
# tests. If your tests depend on any RubyGems, you'll need to tell your users
# to `gem install <%= name %> --development` before they try to
# `gem check --test <%= name %>`.
